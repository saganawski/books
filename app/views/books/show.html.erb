<h1><%= @book.title %></h1>
<h3>Support this page by clicking on the picture and purchasing anything from amazon</h3>

<%= @link %>

<%= render @book.comments %>

<h2>User Upvote Count : <%= @book.votes.count %></h2>
<h2>Add Comment</h2>

<% if !logged_in? %>
	<h2>Log in to add comment</h2>
<% else %>
	<%= render 'votes/vote' %>

	<%= render 'comments/form' %>

<% end %>

